<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@drawable/main_background">


    <ImageView
        android:id="@+id/play_image"
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:layout_marginEnd="130dp"
        android:layout_below="@id/title"
        android:layout_marginTop="40dp"
        android:layout_centerHorizontal="true"
        app:srcCompat="@drawable/play_button"
        android:clickable="true"/>

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="90dp"
        android:fontFamily="@font/wallpoet"
        android:text="@string/title"
        android:textAlignment="center"
        android:textSize="58dp" />

    <ImageView
        android:id="@+id/setting_image"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="10dp"
        app:srcCompat="@drawable/settings"
        android:clickable="true"/>

    <ImageView
        android:id="@+id/sound_on_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_above="@id/setting_image"
        app:srcCompat="@drawable/sound_on_button"
        android:clickable="true"/>

    <ImageView
        android:id="@+id/leaderboard_image"
        android:layout_width="80dp"
        android:layout_height="90dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"
        app:srcCompat="@drawable/leaderboard_1"
        android:clickable="true"/>

</RelativeLayout>